#!/usr/bin/env groovy
pipeline {
agent {
    docker {
          image 'node:6-alpine'
          args '-p 3000:3000'
      }
  }
def app
environment{
  CI='true'
  // DOCKER = tool("testDocker")
}

    stages {
      stage{}
        }
}
